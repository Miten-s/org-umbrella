*/node_modules
*/.env
*/dist
*/build
*/logs/*
.vscode
redis
*/uploads
.agent
.geminiignore
.ecosystem.config.js
deploy.sh

*/.env.*
.agents

# The rules above ignore deploy/.env (correct — it holds real secrets) but the
# `*/.env.*` glob also swallows the committed template. Re-include just that.
!deploy/.env.example